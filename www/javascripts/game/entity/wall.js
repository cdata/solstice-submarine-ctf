define('game/entity/wall',
       ['underscore', 'game/graphic'],
       function(_, Graphic) {
  return Graphic.extend({
    initialize: function() {
      options = _.defaults(options || {}, {
      });
    }
  });
});
